import React from "react";
import SpeakerManager from "@/components/admin/SpeakerManager";

export const metadata = {
  title: "Speaker Management | Admin",
};

export default function SpeakersAdminPage() {
  return <SpeakerManager />;
}
