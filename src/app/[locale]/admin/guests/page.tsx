import React from "react";
import GuestManager from "@/components/admin/GuestManager";

export const metadata = {
  title: "Guest Management | Admin",
};

export default function GuestsAdminPage() {
  return <GuestManager />;
}
